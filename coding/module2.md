1. Print an integer
2. Add two integers
4. Multiply two integers
5. Divide two integers
6. Find remainder
7. Find square of a number
8. Find cube of a number
9. Find average of three integers

10. Find area of a rectangle

Formula: area = length × width

11. Find perimeter of a rectangle

Formula: 2 × (length + width)

12. Swap two integers
13. Check whether number is even or odd
14. Check positive or negative
15. Find the largest of two numbers
16. Find the largest of three numbers

17. Check whether a number is divisible by 5
18. Calculate total and percentage
Question: Take marks of 3 subjects and calculate total and percentage.

19. Calculate area of a circle

Formula: Area = 3.14 × radius × radius

20. Calculate simple interest

Formula: SI = (P × R × T) / 100

21. Calculate temperature Celsius to Fahrenheit

Formula: F = (C × 9/5) + 32


## Operator Precedence
1. Write a C program to input three integers a, b, and c and evaluate:

   result = a + b * c;


2. Write a C program to input three integers a, b, and c and evaluate:

result = a / b + c * a;


3. Write a C program to input four integers a, b, c, and d and evaluate:

result = a + b / c * d;


4. Write a C program to input three integers a, b, and c and calculate the following two expressions:

result1 = a + b * c;
result2 = (a + b) * c;




## 2. Nested if

6. Write a C program to input three integers a, b, and c and find the largest number using nested if.

7. Write a C program to input a student's marks and display the grade using nested if statements.



8. Write a C program to input age and citizenship status and check whether a person is eligible to vote using nested if.




9. Write a C program to input salary and experience and determine whether an employee is eligible for a bonus using nested if.

## 3. switch-case Statement

11. Write a C program to input a number from 1 to 7 and display the corresponding day of the week using switch-case.

12. Write a C program to input a number from 1 to 12 and display the corresponding month name using switch-case.

13. Write a C program to input two integers a and b and an operator op (+, -, *, /) and create a simple calculator using switch-case.

14. Write a C program to input a number from 1 to 5 and display the corresponding menu item using switch-case.

15. Write a C program to input a student's choice and perform the following operations using switch-case:

1 → Calculate total marks
2 → Calculate average marks
3 → Display pass/fail result
4 → Display grade
5 → Exit